services:
  app:
    build: .
    ports:
      - "8020:8000"
    env_file:
      - .env
    environment:
      - PYTHONPATH=/application
    volumes:
      - sqlite_data:/application/app   # чтобы база сохранялась на хосте

volumes:
  sqlite_data: